$(document).ready(() => {
  // initial the side nav
  $(".sidenav").sidenav();

  // initial materialboxed
  $(".materialboxed").materialbox();

  // initial parallex
  $(".parallax").parallax();

  // initial tabs
  $(".tabs").tabs();
});
